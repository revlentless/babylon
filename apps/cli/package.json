{
  "name": "@babylon/cli",
  "version": "0.2.0",
  "private": true,
  "description": "Babylon CLI - Unified command-line interface for db, training, models, games, and agents",
  "type": "module",
  "bin": {
    "babylon": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "cli": "bun run src/index.ts",
    "lint": "biome lint .",
    "typecheck": "tsc --noEmit",
    "db": "bun run src/index.ts db",
    "admin": "bun run src/index.ts admin",
    "status": "bun run src/index.ts status",
    "train": "bun run src/index.ts train",
    "model": "bun run src/index.ts model",
    "game": "bun run src/index.ts game",
    "agent": "bun run src/index.ts agent",
    "generate": "bun run src/index.ts game generate",
    "generate:verbose": "bun run src/index.ts game generate --verbose",
    "world": "bun run src/generate-world.ts",
    "world:verbose": "bun run src/generate-world.ts --verbose",
    "simulate": "bun run src/index.ts game simulate",
    "simulate:batch": "bun run src/index.ts game simulate --count=100 --fast",
    "validate": "bun run src/index.ts game validate",
    "images": "bun run src/generate-actor-images.ts",
    "images:migrate": "bun run src/migrate-images-to-blob.ts"
  },
  "dependencies": {
    "@babylon/agents": "workspace:*",
    "@babylon/api": "workspace:*",
    "@babylon/db": "workspace:*",
    "@babylon/engine": "workspace:*",
    "@babylon/testing": "workspace:*",
    "@babylon/training": "workspace:*",
    "@sentry/bun": "^10.29.0",
    "@fal-ai/client": "^1.7.2",
    "@vercel/blob": "^2.0.0",
    "dotenv": "^16.4.5",
    "ethers": "^6.14.4",
    "nanoid": "^5.1.6",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "bun-types": "^1.3.2",
    "typescript": "^5.9.3"
  }
}
